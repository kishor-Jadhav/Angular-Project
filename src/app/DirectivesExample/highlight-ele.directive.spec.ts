import { HighlightEleDirective } from './highlight-ele.directive';

describe('HighlightEleDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightEleDirective();
    expect(directive).toBeTruthy();
  });
});
